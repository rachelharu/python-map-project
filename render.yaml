services:
  - type: web
    name: spatial-intel-api
    runtime: docker
    plan: free
    healthCheckPath: /
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: CENSUS_API_KEY
        sync: false
      - key: CORS_ORIGINS
        sync: false
